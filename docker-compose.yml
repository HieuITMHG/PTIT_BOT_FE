
version: "3.9"

services:
  frontend:
    build: .
    container_name: ptit_react_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules 
    env_file:
      - .env
